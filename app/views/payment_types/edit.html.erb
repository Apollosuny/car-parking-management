<div class="p-4 sm:ml-64">
  <div class="p-4 dark:border-gray-700 mt-14">
    <div class="flex items-center justify-between mb-6 flex-wrap">
      <h1 class="font-bold text-3xl mb-6">Edit payment_type</h1>
      <% if @payment_type.persisted? %>
      <%= button_to 'Destroy', @payment_type, method: :delete, class: "text-red-700 underline", data: { turbo_confirm: 'Are you sure?' } %>
      <% end %>
    </div>
  
    <%= render 'form', payment_type: @payment_type %>
  </div>
</div>
